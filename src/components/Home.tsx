import React from 'react';
import { Link } from 'react-router-dom';

const Home: React.FC = () => {
  return (
    <main className="global_backfround_gradient">
      <section className='wrapper home_section'>
        Hero
      </section>
    </main>
  );
};

export default Home;